# Alias
if type -q curl
    alias myip 'curl -4 ip.me'
end
if type -q aws-vault
    alias aws-tlac 'aws-vault exec glenon.mateus@proveedor.toyota.com.ar'
end
if type -q bat
    alias cat 'bat -p'
end
alias ip 'ip --color=auto'
